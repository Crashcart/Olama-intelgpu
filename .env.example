# Copy to .env and adjust values before running docker compose

# Host port to expose Ollama on
OLLAMA_PORT=11434

# Ollama Docker image version (use 'latest' or a specific tag like '0.3.14')
OLLAMA_VERSION=latest

# Host path for persistent model storage
# Models downloaded here persist across container restarts/upgrades
OLLAMA_DATA_DIR=/opt/ollama

# (Runtipi only) Model to pull automatically on first container start
# Leave empty to pull models manually later
# Examples: llama3.2:1b  mistral  phi3:mini  codellama:7b
OLLAMA_PULL_MODEL=
